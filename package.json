{
  "name": "reproduce",
  "version": "1.0.0",
  "description": "vue-fullpage nuxt prod build reproduction",
  "author": "Alex Guevara",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "prod": "npm run build && npm start",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "express": "^4.18.2",
    "nuxt": "^2.0.0",
    "vue-fullpage.js": "^0.1.4"
  },
  "devDependencies": {
    "@nuxt/typescript-build": "^0.2.9",
    "nodemon": "^1.18.9",
    "vue-property-decorator": "^8.2.2"
  }
}
